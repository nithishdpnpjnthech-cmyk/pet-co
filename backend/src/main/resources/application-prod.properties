# # Production Configuration for Render
# server.port=${PORT:8081}

# # Database Configuration - Neon PostgreSQL
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-quiet-mud-adzylpm4-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_5w4EMPeZAGFh}
# spring.datasource.driver-class-name=org.postgresql.Driver

# # JPA/Hibernate Configuration for PostgreSQL
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# # File upload configuration
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB

# # Logging Configuration
# logging.level.com.eduprajna=INFO
# logging.level.pet-co=WARN
# logging.level.org.springframework.web=INFO

# # Health check endpoint
# management.endpoints.web.exposure.include=health
# management.endpoint.health.show-details=always

# # Email Configuration (Production)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${EMAIL_USERNAME}
# spring.mail.password=${EMAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# # Application Configuration (Production)
# app.frontend.url=https://pet-cotraditional.in
# app.email.from=${EMAIL_FROM:PET&CO <noreply@pet-cotraditional.in>}